# 用户进程
### TSS
*单核 CPU 要想实现多任务，唯一的方案就是多个任务共享同一个 CPU，也就是只能让 CPU 在多个任务间轮转，让所有任务轮流使用 CPU，除此之外还真别无他法*
TSS 是程序员为任务单独定义的一个结构体变量

### 用户进程
用户进程 = 运行在 CPU 用户态（Ring3）的程序，它：
不能随便访问硬件
不能随便访问内核内存
只能通过 中断 / 系统调用 请求内核帮它干活